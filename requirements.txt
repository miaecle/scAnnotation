# Core single-cell
anndata>=0.10
scanpy>=1.9
scipy>=1.10
numpy>=1.24
pandas>=2.0

# LLM backends (install the ones you use)
anthropic>=0.96      # Claude — pip install anthropic
google-genai>=1.0    # Gemini — pip install google-genai
# openai>=1.0        # GPT   — pip install openai

# Utilities
scikit-learn>=1.3
tqdm
matplotlib
seaborn
jupyter
